#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@goto-bus-stop/envify/bin/envify" "$@"
  ret=$?
else 
  node  "$basedir/../@goto-bus-stop/envify/bin/envify" "$@"
  ret=$?
fi
exit $ret
